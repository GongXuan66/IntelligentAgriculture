# LSTM 土壤湿度预测模型训练依赖
# 在带独显的机器上安装（本脚本不在这台开发机上运行）
# Python 3.9+

# 深度学习框架（GPU 版请按 CUDA 版本到 https://pytorch.org 选择对应安装命令，
# 例如 CUDA 12.x: pip install torch --index-url https://download.pytorch.org/whl/cu121）
# 模型很小（LSTM 64×2），CPU 版 torch 也完全跑得动，几分钟即可训完
torch>=2.0

# 数据处理
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

# 训练曲线绘图
matplotlib>=3.7

# 可选：导出后校验 ONNX 模型与 PyTorch 输出一致性
onnxruntime>=1.16
